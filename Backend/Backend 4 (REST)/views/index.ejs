<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quora Posts</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <h1>Quora Posts</h1>
    <% posts.forEach(post => { %>
        <div class="post">
            <h2 class="user">@<%= post.username %></h2>
            <p class="content"><%= post.content %></p>
            <button><a href="http://localhost:3000/posts/<%= post.id %>">See post in detail</a></button>
            <button><a href="http://localhost:3000/posts/<%= post.id %>/edit">Edit Post</a></button>
            <form method="post" action="/posts/<%= post.id %>?_method=DELETE">
                <button type="submit">Delete Post</button>
            </form>
        </div>
    <% }); %>
    <button><a href="http://localhost:3000/posts/new">Create New Post</a></button>
</body>
</html>